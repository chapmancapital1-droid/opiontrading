import OptionScopeBuilder from "../OptionScopeBuilder";

export const metadata = { title: "Strategy builder — OptionScope" };

export default function BuilderPage() {
  return <OptionScopeBuilder />;
}
